import React from "react";
import "./Inventory.css";
const Inventory = () => {
  const handleInventory = () => {
    //const data = fakeData[0];
    //console.log('before post', fakeData);

  }

  return (
    <div>
      <h1>Add inventory to sell more</h1>
      <button onClick={() => alert("already addeed")}>Add Inventory</button>
    </div>
  );
};
export default Inventory;